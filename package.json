{
  "name": "cultivation-simulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:src": "node build_src.js",
    "build": "node build_src.js",
    "build:module": "node build_src.js",
    "build:dist": "node build_src.js",
    "build:legacy": "node build_ci.js",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  }
}
